[package]
name = "kmiri-helper"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1.0" }
log = "0.4"
env_logger = "0.11"

[package.metadata.rust-analyzer]
rustc_private = true
